using System;
using Neo.FileStorage.Services.Object.Delete.Execute;
using Neo.FileStorage.Services.Reputaion;
using FSObject = Neo.FileStorage.API.Object.Object;

namespace Neo.FileStorage.Services.Object
{
    public static class Helper
    {
        public static FSObject GetObject(ReputationClient client, ExecuteContext context)
        {
            throw new NotImplementedException();
        }
    }
}
